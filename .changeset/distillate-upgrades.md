---
"maturity-skills": patch
---

Distill four autonomous maintenance runs into the existing maturity skills. mutation-tester gains a first-class baseline gate: record the score per target, treat any later drop below baseline as a finding naming the regressed areas, demand apply-fail-revert-verified kills for new-code survivors, and stop active hunting (while keeping the gate) once only cosmetics/equivalents remain (real trajectory: 82.47 -> 92.47 -> 94.12, then hold). emitter-corpus now ships its proven end-state: samples committed to the target repo as permanent CI fixtures with hand-computed semantic expectations and a producer -> upstream-reference README, so every future build reruns them. agent-log-scan gains the detector-anchoring rule (structural anchors over substring scans; the limit-interrupt 89 -> 40 all-true-positive case is canonical) and allowlist conduct rules (read-only/high-frequency/unambiguous, denial-backed proposals only; never writes, deletes, installs, or network sends; settings changes proposed to the human, never applied). `agentlog.py` applies the same anchoring to permission-thrash: a denial now counts only as a tier-1 denial result (`is_error` tool_result starting with a verbatim harness sentinel) or a tier-2 unquoted first-person denial report in free text; doc prose about permissions, the cross-session "denied permission" boilerplate, ALLOW decisions, and tool results quoting denials no longer fire. On the reference corpus, permission-thrash findings dropped 18 -> 9 (all verified genuine, with a results/reports breakdown per finding), and the ground-truth session counts hold (a45187f4: exactly 3; the DurableFunctionsOrchestration classifier denials still flagged).
