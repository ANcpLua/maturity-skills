# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).
Add a changeset (`npx changeset`) with every user-visible change to a skill; the
release workflow turns accumulated changesets into a version PR.
