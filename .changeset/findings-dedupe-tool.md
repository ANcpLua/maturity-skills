---
"maturity-skills": minor
---

maintenance-run: add `scripts/findings.py`, a deterministic, stdlib-only findings-dedupe tool that mechanizes the mechanical share of the dedupe-arbiter step. `dedupe` clusters raw findings by transitive closure over two join rules — root-cause signature token-set overlap >= 1/2 after agentlog-style normalization (paths/ids/hex/numbers stripped), and identical normalized titles — picks each cluster's primary by severity then evidence, merges dimensions, reports every merge edge with its reason, and emits an explicit `ambiguous` list for the LLM arbiter: same-file pairs within a ±15-line window (`--window`) flagged with their line distance, plus similar-but-below-threshold signature pairs. Line proximity deliberately never merges — replayed against maintenance run-002's arbiter ground truth (34 raw findings -> 26 clusters), 4 of 7 same-file near-line pairs were distinct claims and no window setting separates them, while every true near-line pair is captured on the ambiguous list with zero false merges. `validate` schema-checks finder output with actionable per-field errors; `dedupe` warns on stderr when findings omit `root_cause`, which the handoff now requires (title-only output reaches none of the cross-file merges). Exit codes: 0 nothing merged, 2 merges happened, 1 usage/schema errors; same input yields byte-identical output (exact-fraction similarity, no hash-order dependence). SKILL.md step 2 now runs the tool first and treats tool merges as a floor, not a ceiling: the arbiter judges the ambiguous list, may split a tool cluster with a stated reason, and may merge unflagged pairs with stated code evidence — never re-clustering from scratch.
