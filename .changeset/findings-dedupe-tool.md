---
"maturity-skills": minor
---

maintenance-run: add `scripts/findings.py`, a deterministic, stdlib-only findings-dedupe tool that mechanizes the mechanical share of the dedupe-arbiter step. `dedupe` clusters raw findings by transitive closure over three join rules — same normalized file within a ±15-line window (`--window`), root-cause signature token-set overlap >= 1/2 after agentlog-style normalization (paths/ids/hex/numbers stripped), and identical normalized titles — picks each cluster's primary by severity then evidence, merges dimensions, reports every merge edge with its reason, and emits an explicit `ambiguous` list (similar-but-below-threshold pairs), the only part the LLM arbiter still judges. `validate` schema-checks finder output with actionable per-field errors. Exit codes: 0 nothing merged, 2 merges happened, 1 usage/schema errors; same input yields byte-identical output (exact-fraction similarity, no hash-order dependence). SKILL.md step 2 now runs the tool first and restricts the arbiter to the ambiguous list plus split authority with stated reasons — never re-clustering from scratch. Replayed against maintenance run-002's ground truth (34 raw findings -> 26 clusters), the tool reproduces all co-located merges mechanically and flags part of the cross-file residue, which stays with the arbiter.
