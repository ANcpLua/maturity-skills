---
"maturity-skills": minor
---

Add regression-review and maintenance-run, distilled from four autonomous maintenance runs on dotcov. regression-review adversarially reviews only the previous change-set's diff for bugs that change-set itself introduced: diff range derived from the last run/PR/merge, prime-suspect ordering (new invariants, semantic-duplicate helpers, gate disjuncts, exception filters, boundary comparisons), current-code reading around every hunk, concrete failure scenarios required, and a pinning test with every fix — the pass that scored a real self-introduced bug in 3/3 consecutive runs. maintenance-run packages the proven orchestration loop: parallel finders reusing the sibling skills as dimensions, a dedupe arbiter clustering raw findings by root cause before verification, refute-by-default adversarial verifiers whose fix_adjustment overrides the finder's proposal, a fixer fleet with disjoint file ownership in isolated worktrees (two-wave sequencing on API dependencies, per-fixer green gate, rejection-is-terminal conduct rules), single-writer integration, a run report with the findings-curve datapoint, regression-review as the next run's opener, and a hard termination rule: when a run's findings are predominantly regressions of the previous run's own changes, the target is done.
